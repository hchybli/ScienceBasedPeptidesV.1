# Legacy folder

Product art lives only in `public/products/*.png` and is used **as-is** (transparent PNG). The site no longer uses separate showcase overrides or gradient baking.

You can delete any leftover PNGs here; they are ignored.
